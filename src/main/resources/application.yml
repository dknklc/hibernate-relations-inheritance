
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hibernateDb
    username: user
    password: user123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect